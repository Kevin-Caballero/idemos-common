{
  "name": "@idemos/common",
  "version": "1.0.0",
  "description": "IDemos — shared entities, DTOs and interfaces",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "typeorm": "^0.3.0"
  }
}
